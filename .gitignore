.idea

# excluding state so if others run this in their AWS accounts it doesn't screw with their state
.terraform.lock.hcl
.terraform
terraform.tfstate*
tfplan

# ssh key stuff
*ssh*

# Ignore zip files that we don't want in version control
*.zip

forge-v4